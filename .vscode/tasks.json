{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build dygram",
            "command": "npm run langium:generate && npm run build",
            "type": "shell",
            "group": {
                "kind": "build",
            },
            "detail": "Langium: Generate grammar and build the machine language",
            "icon": {
                "color": "terminal.ansiGreen",
                "id": "server-process"
            }
        }, {
            "label": "generate mermaid ouput ",
            "command": "dygram generate-mermaid ${file} -d ${fileDirname}/out",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}
