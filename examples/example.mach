machine "Example"

init {
    query: "string";
};
analyse;
tool feedback {
    retry<int>: 4;
};
complete;
tool another;

init --> analyse;
analyse -- next --> another;
analyse --> feedback;
feedback --> complete;
