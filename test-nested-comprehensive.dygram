machine "Comprehensive nested attributes test"

config: {
  api: {
    url: "https://api.example.com";
    timeout: 5000;
    retry: true;
  };
  features: {
    caching: false;
    logging: "verbose";
  };
};

tags: ["api", "backend", "production"];

node ServiceA {
  enabled: true;
  priority: 10;
  metadata: {
    version: "1.2.3";
    author: "system";
    tags: ["critical", "monitored"];
  };
}

node ServiceB {
  ports: [8080, 8081, 8082];
  config: {
    retries: 3;
    timeout: 30;
  };
}
