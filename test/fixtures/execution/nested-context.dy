// Test 2.2: Nested Context Access
machine "Nested Context"

context Database {
    host: "localhost";
    port: 5432;
}

context Settings {
    env: "production";
}

task Start @start {
    connectionString: "{{ Database.host }}:{{ Database.port }}";
    environment: "{{ Settings.env }}";
}
